/**
 * 表单点击事件
 */
export function tick(col:any) {
  col.value == "√" ? col.value = "" : col.value = "√"
  return true
}

export function openTimePicker(col:any) {
  col.value == "√" ? col.value = "" : col.value = "√"
  return true
}