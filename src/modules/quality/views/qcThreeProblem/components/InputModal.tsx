import React from 'react'
import styled from 'styled-components'
import { observer } from 'mobx-react'

export default observer(function InputModal(props) {
  return (
    <Wrapper>

    </Wrapper>
  )
})

const Wrapper = styled.div`

`